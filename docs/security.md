# Seguridad

Reglas:
- No se suben IPs reales, usuarios, passwords, CSV reales ni evidencia a GitHub.
- `examples/` solo contiene datos dummy.
- Cualquier log debe redactar credenciales.

Buenas prácticas:
- Preferir `credential_id` y resolver credenciales fuera del repo (env/secret store).
